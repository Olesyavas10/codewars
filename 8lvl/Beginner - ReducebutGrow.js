/* Given a non-empty array of integers, return the result of multiplying the values together in order. Example:

 */
function grow(x){
  let multiply=0;
return multiply=x.reduce((acc,mult)=>acc*mult, acc=1)
}